
(1) Port `lmsmodsearch.py` to `lmstrix desc` which should use LLM to: 

(a) Describe each model in freeform text

(b) Assign 0–3 keywords from each of the categories: `arch`, `inp`, `outp`, `proc` using the following vocabulary: 

- `arch-dense`: dense model architecture
- `arch-gguf`: cross-platform GGUF format
- `arch-mlx`: macOS-only MLX format
- `arch-moe`: mixture of experts model architecture, more efficient than dense
- `inp-audio`: accepts various audio as input
- `inp-image`
- `inp-instruct`
- `inp-multiling`
- `inp-speech`
- `outp-audio`
- `outp-caption`
- `outp-chat`
- `outp-code`
- `outp-data`
- `outp-docstruct`
- `outp-image`
- `outp-ocr`
- `outp-speech`
- `outp-text`
- `outp-tldr`
- `outp-translat`
- `proc-agentic`
- `proc-longctx`
- `proc-thinking`

(3) The output of the LLM should look like: 

```json
{ 
    "EZCon/GLM-OCR-8bit-mlx": {
        "id": "glm-ocr-mlx",
        "description": "GLM-OCR-8bit-mlx is a 0.9 billion parameter multimodal vision-language model based on the GLM-V encoder-decoder architecture, designed specifically for optical character recognition and complex document understanding tasks. It features a CogViT visual encoder paired with a lightweight cross-modal connector and GLM-0.5B language decoder, optimized with Multi-Token Prediction loss and 8-bit quantization for efficient 1.58GB deployment on Apple Silicon via the MLX framework. The model excels at extracting text, tables, formulas, and structured data from complex layouts including PDFs and noisy scans, achieving state-of-the-art OmniDocBench performance while supporting outputs in Markdown, JSON, and LaTeX formats for document parsing and information extraction applications.",
        "keywords": [
            "arch-dense", "arch-mlx", "inp-image", "outp-ocr", "outp-docstruct", "outp-text"
        ]
    }
}
```

(2) So you need to write the prompt accordingly. 

(3) `lmstrix desc` should write the descriptions and keywords back into the original `lmstrix.json`

(4) `lmstrix list` should accept `--show md` and then the output should be not a table but a longer Markdown report that includes the full description and keywords for each model. 

(5) `lmstrix list` and `lmstrix test` should accept `--key keyword1,keyword2` and then process only the models that match all keywords

(6) `lmstrix list` should accept `--sort arch` or `archd` or `inp` or `inpd` or `outp` or `outpd` or `proc` or `procd` and which would sort according to the keywords in that category (descending for `d` suffix), and then within each using 'smart' or 'smartd' accordingly

